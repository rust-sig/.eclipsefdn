site_name: Otterdog configuration @ rust-sig
site_description: Otterdog configuration @ rust-sig
strict: false

site_url: https://rust-sig.github.io/.eclipsefdn/

repo_name: rust-sig/.eclipsefdn
repo_url: https://github.com/rust-sig/.eclipsefdn/

docs_dir: ./docs
site_dir: ./_site

theme:
  name: 'material'
  custom_dir: docs/overrides
  features:
    - navigation.tabs

markdown_extensions:
  - pymdownx.superfences
  - pymdownx.tabbed:
      alternate_style: true
  - attr_list
  - pymdownx.emoji:
      emoji_index: !!python/name:materialx.emoji.twemoji
      emoji_generator: !!python/name:materialx.emoji.to_svg

plugins:
  - macros

nav:
  - Overview: index.md

watch: []

extra:
  org_id: rust-sig

extra_css:
  - stylesheets/extra.css
